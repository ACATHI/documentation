<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Cron diari de comprovació de terminis -->
        <record id="ir_cron_grant_deadline_check" model="ir.cron">
            <field name="name">Grant Management: Comprovació de terminis</field>
            <field name="model_id" ref="grant_management.model_grant_application"/>
            <field name="state">code</field>
            <field name="code">model._cron_check_deadlines()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active">True</field>
            <field name="user_id" ref="base.user_root"/>
        </record>

    </data>
</odoo>
